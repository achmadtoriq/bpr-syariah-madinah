---
# Konfigurasi Auto-Deployment cPanel Git Version Control
deployment:
  tasks:
    - export DEPLOYPATH=/home/bprsmadi/repositories/bpr-syariah-madinah
    - cd $DEPLOYPATH
    - chmod +x deploy.sh
    - ./deploy.sh
